# Add Back to Landing Link
>
> Goal: Allow users to return to the landing page from login screens.
> Architecture: Absolute positioned navigation links in login pages.

### Task 1: [x] ClientLoginPage.tsx — Add Back Link

1. Update `src/features/auth/pages/client/ClientLoginPage.test.tsx` to check for the presence of the home link.
2. Implement the link in `ClientLoginPage.tsx`.
3. Run tests: `cd peluqueria-client && npm test ClientLoginPage.test.tsx`.
4. Verify: tests pass ✅.
5. `git commit` + `/checkpoint`.

### Task 2: [x] ProLoginPage.tsx — Add Back Link

1. Update `src/features/auth/pages/pro/ProLoginPage.test.tsx` to check for the presence of the home link.
2. Implement the link in `ProLoginPage.tsx`.
3. Run tests: `cd peluqueria-client && npm test ProLoginPage.test.tsx`.
4. Verify: tests pass ✅.
5. `git commit` + `/checkpoint`.
